---
import type { HTMLAttributes, HTMLTag } from 'astro/types';

interface Props extends HTMLAttributes<'div' | 'section' | 'aside'> {
  tag?: HTMLTag;
}

const { tag: Element = 'section', ...attr } = Astro.props as Props;
---

<Element class:list={[attr.class]} {...attr}>
  <slot />
</Element>
